version: '2'
services:

  bpa-ckan:
    build:
      context: ./
      dockerfile: Dockerfile
      args:
        ARG_PIP_INDEX_URL: ${PIP_INDEX_URL}
        ARG_PIP_TRUSTED_HOST: ${PIP_TRUSTED_HOST}
        http_proxy:
    image: muccg/bpa-ckan
